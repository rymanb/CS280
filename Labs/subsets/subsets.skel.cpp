#include "subsets.h"

// actual recursive function
template< typename T >
void subset_rec( ..... ) 
{
}

// kickstart
template< typename T >
void subsets( std::vector<T> const& set)
{
    subset_rec( .... );
}
